# AGENTS.md

이 문서는 `badaoncom-sketch/hmgrill` 저장소에서 작업하는 에이전트와 개발자가 따라야 할 운영 지침이다.

## 프로젝트 맥락

화목 홈페이지는 단일 매장 운영을 위한 공식 홈페이지이다.

핵심 기능은 회원가입 기반 QR 쿠폰 발행, 다운로드, 매장 사용 처리 시스템이다. POS 연동은 하지 않으며, 직원이 POS에서 수동 할인 처리 후 태블릿 직원모드에서 쿠폰을 사용완료 처리한다.

프랜차이즈 기능은 현재 범위에 포함하지 않는다.

## 기준 문서

작업 전 아래 문서를 먼저 확인한다.

- [README.md](./README.md): 프로젝트 개요와 현재 상태
- [화목-홈페이지-v1.0-기획설계서.md](./화목-홈페이지-v1.0-기획설계서.md): 제품 요구사항과 운영 정책의 최상위 기준
- [docs/IMPLEMENTATION-ROADMAP.md](./docs/IMPLEMENTATION-ROADMAP.md): 개발 순서와 검증 기준
- [docs/FILE-AUDIT.md](./docs/FILE-AUDIT.md): 파일별 점검 기록

문서와 구현이 충돌하면 기획설계서를 우선 확인한다. 구현 변경으로 요구사항이 달라지는 경우에는 관련 문서를 함께 갱신한다.

## 개발 원칙

- 코딩 시 이모지를 사용하지 않는다.
- 하드코딩을 피하고 설정값은 환경변수 또는 설정 파일로 관리한다.
- 테스트용 임시 코드와 사용하지 않는 기능 선구현은 금지한다.
- 재사용 가능한 컴포넌트와 명확한 모듈 경계를 우선한다.
- 공식 문서 확인 없이 추측 구현하지 않는다.
- 실제 운영 가능한 구조를 기준으로 구현한다.
- 쿠폰, 다운로드, 사용, 만료, 중단, 재발행 이력은 물리 삭제하지 않고 상태와 이력으로 관리한다.

## 예정 기술 기준

- Next.js 16
- App Router
- TypeScript
- Tailwind CSS
- Shadcn/ui
- Supabase
- PostgreSQL
- Supabase Auth
- Supabase Storage
- Resend
- Vercel

구현 전 각 도구의 최신 공식 문서를 확인한다.

## 기능 범위

반드시 포함할 핵심 범위:

- 회원가입 및 이메일 인증
- QR 쿠폰 발행, 다운로드, 사용 처리
- 회원 쿠폰함 및 사용내역
- 관리자 쿠폰, 회원, 직원, 메뉴, 이벤트, 공지 관리
- 태블릿 브라우저 기반 직원모드
- 쿠폰 다운로드, 사용, 만료, 재발행 이력 관리

현재 제외 범위:

- POS 직접 연동
- 별도 Android 앱
- 프랜차이즈 관리 기능

## 권한과 데이터 원칙

- 회원, 직원, 관리자의 권한을 명확히 분리한다.
- 인증 전 회원은 쿠폰 다운로드와 마이페이지 접근을 제한한다.
- 관리자 기능은 관리자 권한 없이는 접근할 수 없어야 한다.
- 직원모드는 계산대 태블릿 사용 흐름에 최적화한다.
- QR에는 회원정보나 쿠폰 금액을 직접 넣지 않고 쿠폰 고유 토큰만 포함한다.
- 쿠폰 정보와 사용 가능 여부는 서버에서 조회한다.
- 재사용 방지는 서버 트랜잭션과 상태 검증으로 보장한다.

## 구현 순서

기본 순서는 `docs/IMPLEMENTATION-ROADMAP.md`를 따른다.

1. 프로젝트 기반 구성
2. 데이터 모델과 인증 설계
3. 쿠폰 발행 및 다운로드
4. 회원 쿠폰 화면
5. 직원모드
6. 관리자 운영 기능
7. 운영 검증과 배포

단계를 건너뛰어 구현해야 하는 경우에는 이유와 검증 범위를 문서에 남긴다.

## 검증 원칙

구현 파일이 생긴 뒤에는 변경 범위에 맞게 아래 검증을 수행한다.

- 의존성 설치 상태 확인
- 린트
- 타입 체크
- 프로덕션 빌드
- 핵심 라우트 접근 확인
- 인증과 권한 흐름 확인
- 쿠폰 발행, 다운로드, 사용완료, 만료, 재발행 흐름 확인
- Supabase RLS 정책 확인
- 환경변수 누락 여부 확인

아직 구현 코드가 없는 문서화 단계에서는 다음을 확인한다.

- 문서 파일 존재 여부
- 문서 간 링크 유효성
- 기획설계서와 README, 로드맵의 내용 충돌 여부
- Git 상태와 원격 저장소 동기화 여부

## Git 작업 원칙

- 작업 전 `git status --short --branch`로 현재 상태를 확인한다.
- 사용자가 만든 변경사항을 임의로 되돌리지 않는다.
- 관련 없는 파일은 커밋에 포함하지 않는다.
- 커밋 메시지는 변경 내용을 짧고 명확하게 적는다.
- GitHub에 푸시한 뒤 로컬 브랜치와 `origin` 브랜치가 같은 커밋을 가리키는지 확인한다.

## Supabase 작업 원칙

- Supabase 작업 전 changelog, 공식 문서, CLI help를 확인한다.
- 새 마이그레이션 파일은 `supabase migration new <name>`으로 생성한다.
- `public` 스키마 테이블은 명시적 `GRANT`와 RLS 정책을 함께 작성한다.
- `service_role` 키는 서버 환경에서만 사용하고 클라이언트 코드에 노출하지 않는다.
- 사용자가 SQL 적용을 요청하면 Codex가 직접 마이그레이션을 실행하고 결과를 검증한다.

## 문서 작성 규칙

- 운영 지침과 프로젝트 설명은 한국어로 작성한다.
- 명령어, 파일명, 경로, 환경변수, 코드 식별자는 원문 표기를 유지한다.
- 근거 없는 기능 확장이나 과장된 표현을 피한다.
- 변경사항이 요구사항, 구현 순서, 검증 기준에 영향을 주면 관련 문서를 함께 갱신한다.
